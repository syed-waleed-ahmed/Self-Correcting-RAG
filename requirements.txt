# Convenience installer. Installs the package plus the local embedding stack
# (sentence-transformers + torch). For development, prefer:
#     pip install -e ".[local,dev]"
# The canonical dependency list lives in pyproject.toml.
-e .[local]
